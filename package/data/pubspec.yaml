name: data
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.0.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  domain:
    path: ../domain

  http: ^1.1.0
  injectable: ^2.3.0
  get_it: 7.6.4
  json_serializable: ^6.7.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  flutter_gen_runner:
  injectable_generator: ^2.1.6
  flutter_lints: ^2.0.3

flutter:
  assets:
    - assets/data/en/
    - assets/data/uk/
